{
  "api_id": "azure.com:cognitiveservices-LUIS-Runtime:v2.0 preview",
  "endpoint_count": 1,
  "endpoints": [
    {
      "id": "get-appid",
      "name": "GET /{appId}",
      "description": "Gets predictions for a given utterance, in the form of intents and entities. The current maximum query size is 500 characters.",
      "method": "GET",
      "url": "https://azure.local/luis/v2.0/apps/{{APPID}}",
      "headers": {
        "Ocp-Apim-Subscription-Key": "{{AZURE_COM_API_KEY}}"
      },
      "query_params": {
        "q": ""
      },
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{APPID}}",
        "{{AZURE_API_KEY}}",
        "{{AZURE_COM_API_KEY}}"
      ],
      "notes": "Replace {{AZURE_API_KEY}} with your AZURE API KEY. Replace {{APPID}} with a real appid."
    }
  ]
}