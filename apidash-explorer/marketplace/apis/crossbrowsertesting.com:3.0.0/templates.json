{
  "api_id": "crossbrowsertesting.com:3.0.0",
  "endpoint_count": 3,
  "endpoints": [
    {
      "id": "compare-screenshot-test-versions",
      "name": "Compare Screenshot Test Versions",
      "description": "Get comparison results for all browsers in target screenshot test against the same browser in the base screenshot test. This is a good method for regression testing. For example, you've run a screenshot test against a set of browsers that is \"good\". Then, after some changes, you run a new screenshot test against the same set of browsers. This method will compare each of the same browsers against each other. For example, IE9 will be compared to IE9 from an earlier test. This is a many-to-many comparison where the OS/Browser/Resolution must match between the two test versions in order for the comparison to return results. The two versions can be from the same screenshot_test_id or not.",
      "method": "GET",
      "url": "https://crossbrowsertesting.com/api/v3/screenshots/{{TARGET_SCREENSHOT_TEST_ID}}/{{TARGET_VERSION_ID}}/comparison/parallel/{{BASE_VERSION_ID}}",
      "headers": {
        "Authorization": "Basic{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      },
      "query_params": {},
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{TARGET_VERSION_ID}}",
        "{{BASE_VERSION_ID}}",
        "{{CROSSBROWSERTESTING_USERNAME}}",
        "{{TARGET_SCREENSHOT_TEST_ID}}",
        "{{CROSSBROWSERTESTING_PASSWORD}}",
        "{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      ],
      "notes": "Replace {{CROSSBROWSERTESTING_USERNAME}} with your CROSSBROWSERTESTING USERNAME. Replace {{CROSSBROWSERTESTING_PASSWORD}} with your CROSSBROWSERTESTING PASSWORD. Replace {{TARGET_SCREENSHOT_TEST_ID}} with a real target_screenshot_test_id. Replace {{TARGET_VERSION_ID}} with a real target_version_id. Replace {{BASE_VERSION_ID}} with a real base_version_id."
    },
    {
      "id": "compare-full-screenshot-test",
      "name": "Compare Full Screenshot Test",
      "description": "Get comparison results for all browsers in target screenshot test against a base screenshot result. The base result can be from the same test or from another test run at an earlier time. This is a one-to-many comparison.",
      "method": "GET",
      "url": "https://crossbrowsertesting.com/api/v3/screenshots/{{TARGET_SCREENSHOT_TEST_ID}}/{{TARGET_VERSION_ID}}/comparison/{{BASE_RESULT_ID}}",
      "headers": {
        "Authorization": "Basic{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      },
      "query_params": {},
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{TARGET_VERSION_ID}}",
        "{{CROSSBROWSERTESTING_USERNAME}}",
        "{{TARGET_SCREENSHOT_TEST_ID}}",
        "{{CROSSBROWSERTESTING_PASSWORD}}",
        "{{BASE_RESULT_ID}}",
        "{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      ],
      "notes": "Replace {{CROSSBROWSERTESTING_USERNAME}} with your CROSSBROWSERTESTING USERNAME. Replace {{CROSSBROWSERTESTING_PASSWORD}} with your CROSSBROWSERTESTING PASSWORD. Replace {{TARGET_SCREENSHOT_TEST_ID}} with a real target_screenshot_test_id. Replace {{BASE_RESULT_ID}} with a real base_result_id. Replace {{TARGET_VERSION_ID}} with a real target_version_id."
    },
    {
      "id": "compare-single-screenshot",
      "name": "Compare Single Screenshot",
      "description": "Get comparison results for a single target screenshot result against a base screenshot result. This is a one-to-one comparison.",
      "method": "GET",
      "url": "https://crossbrowsertesting.com/api/v3/screenshots/{{TARGET_SCREENSHOT_TEST_ID}}/{{TARGET_VERSION_ID}}/{{TARGET_RESULT_ID}}/comparison/{{BASE_RESULT_ID}}",
      "headers": {
        "Authorization": "Basic{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      },
      "query_params": {},
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{TARGET_RESULT_ID}}",
        "{{TARGET_VERSION_ID}}",
        "{{CROSSBROWSERTESTING_USERNAME}}",
        "{{TARGET_SCREENSHOT_TEST_ID}}",
        "{{CROSSBROWSERTESTING_PASSWORD}}",
        "{{BASE_RESULT_ID}}",
        "{{CROSSBROWSERTESTING_COM_BASE64_CREDENTIALS}}"
      ],
      "notes": "Replace {{CROSSBROWSERTESTING_USERNAME}} with your CROSSBROWSERTESTING USERNAME. Replace {{CROSSBROWSERTESTING_PASSWORD}} with your CROSSBROWSERTESTING PASSWORD. Replace {{TARGET_SCREENSHOT_TEST_ID}} with a real target_screenshot_test_id. Replace {{BASE_RESULT_ID}} with a real base_result_id. Replace {{TARGET_RESULT_ID}} with a real target_result_id. Replace {{TARGET_VERSION_ID}} with a real target_version_id."
    }
  ]
}