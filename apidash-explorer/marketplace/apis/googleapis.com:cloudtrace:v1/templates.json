{
  "api_id": "googleapis.com:cloudtrace:v1",
  "endpoint_count": 3,
  "endpoints": [
    {
      "id": "get-v1-projects-projectid-traces",
      "name": "GET /v1/projects/{projectId}/traces",
      "description": "Returns a list of traces that match the specified filter conditions.",
      "method": "GET",
      "url": "https://cloudtrace.googleapis.com/v1/projects/{{PROJECTID}}/traces",
      "headers": {
        "Authorization": "Bearer{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      },
      "query_params": {},
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{PROJECTID}}",
        "{{GOOGLEAPIS_ACCESS_TOKEN}}",
        "{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      ],
      "notes": "Replace {{GOOGLEAPIS_ACCESS_TOKEN}} with your GOOGLEAPIS ACCESS TOKEN. Replace {{PROJECTID}} with a real projectid."
    },
    {
      "id": "patch-v1-projects-projectid-traces",
      "name": "PATCH /v1/projects/{projectId}/traces",
      "description": "Sends new traces to Cloud Trace or updates existing traces. If the ID of a trace that you send matches that of an existing trace, any fields in the existing trace and its spans are overwritten by the provided values, and any new fields provided are merged with the existing trace data. If the ID does not match, a new trace is created.",
      "method": "PATCH",
      "url": "https://cloudtrace.googleapis.com/v1/projects/{{PROJECTID}}/traces",
      "headers": {
        "Content-Type": "application/json",
        "Authorization": "Bearer{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      },
      "query_params": {},
      "body": {
        "traces": []
      },
      "sample_response": {},
      "auth_placeholders": [
        "{{PROJECTID}}",
        "{{GOOGLEAPIS_ACCESS_TOKEN}}",
        "{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      ],
      "notes": "Replace {{GOOGLEAPIS_ACCESS_TOKEN}} with your GOOGLEAPIS ACCESS TOKEN. Replace {{PROJECTID}} with a real projectid."
    },
    {
      "id": "get-v1-projects-projectid-traces-traceid",
      "name": "GET /v1/projects/{projectId}/traces/{traceId}",
      "description": "Gets a single trace by its ID.",
      "method": "GET",
      "url": "https://cloudtrace.googleapis.com/v1/projects/{{PROJECTID}}/traces/{{TRACEID}}",
      "headers": {
        "Authorization": "Bearer{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      },
      "query_params": {},
      "body": null,
      "sample_response": {},
      "auth_placeholders": [
        "{{PROJECTID}}",
        "{{TRACEID}}",
        "{{GOOGLEAPIS_ACCESS_TOKEN}}",
        "{{GOOGLEAPIS_COM_ACCESS_TOKEN}}"
      ],
      "notes": "Replace {{GOOGLEAPIS_ACCESS_TOKEN}} with your GOOGLEAPIS ACCESS TOKEN. Replace {{PROJECTID}} with a real projectid. Replace {{TRACEID}} with a real traceid."
    }
  ]
}